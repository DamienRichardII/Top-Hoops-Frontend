/* =====================================================================
   TOP HOOPS — Configuration front
   URL du backend (Railway).
   En local, tu peux forcer http://localhost:3001 en définissant
   window.TOP_HOOPS_API_URL avant le chargement de ce script.
   ===================================================================== */
window.TOP_HOOPS_API_URL = window.TOP_HOOPS_API_URL || "https://top-hoops-backend-production.up.railway.app";
